# Задание 1
## Задача 2
### Текст задачи
Дана сигнатура метода: public int sumLastNums (int x); 
Необходимо реализовать метод таким образом, чтобы он возвращал результат 
сложения двух последних знаков числах, предполагая, что знаков в числе не 
менее двух.
### Алгоритм решения
После ввода числа (с помощью класса Scanner и метода nextInt()) проверяем, что оно корректно, в методе «getInput» с помощью метода «hasNextInt()», которое должно вернуть «true», чтобы задача была решена верно. Далее берем от числа модуль, так как сумма двух последних знаков числа не зависит от того, какое число: больше 0 или нет. В методе инициализируем переменную для суммы, находим последнюю и предпоследнюю цифру числа c помощью деления нацело и с остатком, далее складываем и возвращаем полученное значение. Далее в main вызываем метод.

## Задача 4
### Текст задачи
Дана сигнатура метода: public bool isPositive (int x); 
Необходимо реализовать метод таким образом, чтобы он принимал число x и 
возвращал «true», если оно положительное. 
### Алгоритм решения
После ввода числа (c помощью класса Scanner и метода nextInt()) проверяем, что оно корректно, в методе «getInput» с помощью метода «hasNextInt()», которое должно вернуть true, чтобы задача была решена верно. И в методе проверяем, если число > 0, то возвращаем «true», иначе «false». Далее в main вызываем метод.

## Задача 6
### Текст задачи
Дана сигнатура метода: public bool isUpperCase (char x); 
Необходимо реализовать метод таким образом, чтобы он принимал символ x и 
возвращал true, если это большая буква в диапазоне от ‘A’ до ‘Z’.  
### Алгоритм решения
Вводим символ с помощью класса Scanner и методов next() и charAt(). В методе проверяем, если символ «больше или равен» символу ‘A’ и «меньше или равен символу ‘Z’, то есть находится в диапазоне между ними, то возвращаем «true», иначе «false». Далее в main вызываем метод.

## Задача 8
### Текст задачи
Дана сигнатура метода: public bool isDivisor (int a, int b); 
Необходимо реализовать метод таким образом, чтобы он возвращал true, если 
любое из принятых чисел делит другое нацело.
### Алгоритм решения
После ввода числел (c помощью класса Scanner и метода nextInt()) проверяем, что они корректны, в методе «getInput» с помощью метода «hasNextInt()», которое должно вернуть true, чтобы задача была решена верно. В методе проверяем, что деление с остатком любого веденного числа на другое равно 0 и что делитель в выражении не равен 0. Если это условие выполняется возвращаем «true», иначе «false». Далее в main вызываем метод.

## Задача 10
### Текст задачи
Дана сигнатура метода: public int lastNumSum(int a, int b) 
Необходимо реализовать метод таким образом, чтобы он считал сумму цифр 
двух чисел из разряда единиц. Выполните с его помощью последовательное 
сложение пяти чисел и результат выведите на экран. Постарайтесь выполнить 
задачу, используя минимально возможное количество вспомогательных 
переменных.
### Алгоритм решения
Сначала в цикле вводим 5 чисел, проверяем, что они корректны, в методе «getInput» с помощью метода «hasNextInt()», и для каждого ищем сумму его единиц и предыдущей суммы, которая находится в методе. В методе возвращаем просто сумму количества единиц первого числа и второго. Далее в main вызываем метод.

# Задание 2
## Задача 2
### Текст задачи
Дана сигнатура метода: public double safeDiv (int x, int y); 
Необходимо реализовать метод таким образом, чтобы он возвращал деление x 
на y, и при этом гарантировал, что не будет выкинута ошибка деления на 0. При 
делении на 0 следует вернуть из метода число 0. Подсказка: смотри 
ограничения на операции типов данных.
### Алгоритм решения
Сначала вводим 2 числа, проверяем, что они корректны, в методе «getInput» с помощью метода «hasNextInt()». В методе проверяем, если y не равен 0, то возвращаем значение деления х на у в виде числа с плавающей точкой, иначе возвращаем 0. Далее в main вызываем метод.

## Задача 4
### Текст задачи
Дана сигнатура метода: public String makeDecision (int x, int y); 
Необходимо реализовать метод таким образом, чтобы он возвращал строку, 
которая включает два принятых методом числа и корректно выставленный 
знак операции сравнения (больше, меньше, или равно).
### Алгоритм решения
Сначала вводим 2 числа, проверяем, что они корректны, в методе «getInput» с помощью метода «hasNextInt()». В методе проверяем, если х > y, то вернем строку со знаком операции «>», если х < y, то вернем строку со знаком операции «<», в оставшемся случае вернем строку со знаком «=». Далее в main вызываем метод.

## Задача 6
### Текст задачи
Дана сигнатура метода: public bool sum3 (int x, int y, int z); 
Необходимо реализовать метод таким образом, чтобы он возвращал true, если 
два любых числа (из трех принятых) можно сложить так, чтобы получить 
третье. 
### Алгоритм решения
Сначала вводим 3 числа, проверяем, что они корректны, в методе «getInput» с помощью метода «hasNextInt()». Проверяем в методе, если сумма переменных х и у равна z, или сумма переменных х и z равна y, или сумма переменных y и z равна x, то возвращаем «true», иначе вернем «false». Далее в main вызываем метод.

## Задача 8
### Текст задачи
Дана сигнатура метода: public String age (int x); 
Необходимо реализовать метод таким образом, чтобы он возвращал строку, в 
которой сначала будет число х, а затем одно из слов: год, года,лет 
Слово “год” добавляется, если число х заканчивается на 1, кроме числа 11. 
Слово “года” добавляется, если число х заканчивается на 2, 3 или 4, кроме чисел 
12, 13, 14. 
Слово “лет” добавляется во всех остальных случаях.
### Алгоритм решения
Сначала вводим число, проверяем, что оно корректно и больше 0, в методе «test» с помощью метода «hasNextInt()». В методе находим последнюю цифру введенного числа с помощью деления с остатком. Далее проверяем это число, если оно равно 1 и при этом не равно 11, то выведем строку со словом «год», если число равно 2, или 3, или 4, но при этом оно не равно 12, 13, 14, то выведем строку со словом «года», в остальных случаях выведем строку со словом «лет». Далее в main вызываем метод.

## Задача 10
### Текст задачи
Дана сигнатура метода: public void printDays (String x); 
В качестве параметра метод принимает строку, в которой записано название 
дня недели. Необходимо реализовать метод таким образом, чтобы он выводил 
на экран название переданного в него дня и всех последующих до конца недели 
дней. Если в качестве строки передан не день, то выводится текст “это не день 
недели”. Первый день понедельник, последний – воскресенье. Вместо if в данной 
задаче используйте switch.
### Алгоритм решения
Сначала вводим название дня недели без проверок в main, так как эта проверка будет осуществляться по условию задачи в switch в методе. В методе в switch в case прописываем сразу два варианта написания дней недели: с большой и маленькой буквы для удобства. В каждом case выводим это же название дня недели, как и в заголовке, при этом не прописываем break, чтобы вывод не останавливался, пока не закончатся case. В ошибку заносим сообщение «это не день недели». Далее в main вызываем метод.

# Задание 3
## Задача 2
### Текст задачи
Дана сигнатура метода: public String reverseListNums (int x); 
Необходимо реализовать метод таким образом, чтобы он возвращал строку, в 
которой будут записаны все числа от x до 0 (включительно).
### Алгоритм решения
Сначала вводим число, проверяем, что оно корректно, в методе «getInput» с помощью метода «hasNextInt()». В методе инициализируем пустую строку и  рассматриваем два варианта ввода числа х: оно больше 0, оно меньше 0. Если число больше 0, то в цикле проходим от значения равного введенному числу до того момента, когда оно станет больше или равно 0 и каждый раз прибавляем к строке текущее число. Если число отрицательное, то так же в цикле идет от х до 0, но уже должно быть меньше или равно. И каждый раз прибавляем к строке текущее число. В итоге возвращаем полученную строку. Далее в main вызываем метод.

## Задача 4
### Текст задачи
Дана сигнатура метода: public int pow (int x, int y); 
Необходимо реализовать метод таким образом, чтобы он возвращал результат 
возведения x в степень y.  
### Алгоритм решения
Сначала вводим 2 числа, проверяем, что они корректно, в методе «getInput» с помощью метода «hasNextInt()». В методе создаем переменную, хранящую результат, равную 1, для решения по условию. Далее для каждого числа от 0 до у выполняем умножение переменной, где хранится результат, на число х, то есть получается, что 1 умножается на число х у раз. Далее в main вызываем метод.

## Задача 6
### Текст задачи
Дана сигнатура метода: public bool equalNum (int x); 
Необходимо реализовать метод таким образом, чтобы он возвращал true, если 
все знаки числа одинаковы, и false в ином случае. 
### Алгоритм решения
Сначала вводим число, проверяем, что оно корректно, в методе «getInput» с помощью метода «hasNextInt()». Далее берем от числа модуль, так как знаки числа не зависят от того, какое число: больше 0 или нет. В методе с самого начала необходимо, запомнив, отделить от числа его последнюю цифру для более легкого сравнения. И каждый раз запоминать последнюю цифру оставшегося числа, убирая ее, пока оно больше 0, и во время этого следует проверять, равны ли эти цифры. Если нет, то возвращаем «false», иначе вернем «true». Далее в main вызываем метод.

## Задача 8
### Текст задачи
Дана сигнатура метода: public void leftTriangle (int x); 
Необходимо реализовать метод таким образом, чтобы он выводил на экран 
треугольник из символов ‘*’ у которого х символов в высоту, а количество 
символов в ряду совпадает с номером строки.
### Алгоритм решения
Сначала вводим число, проверяем, что оно корректно и больше 0, в методе «test» с помощью метода «hasNextInt()». Чтобы вывести елочку необходимо построить некий неполный двумерный массив. Для этого в цикле для высоты (количества строк) пройдем по каждому числу от 1 до х, для количества символов в ряду (количества столбцов) пройдем по каждой строке из предыдущего цикла. И выведем звездочку. И чтобы они не находились все в одной строке, воспользуемся «System.out.println».  Далее в main вызываем метод.

## Задача 10
### Текст задачи
Дана сигнатура метода: public void guessGame()  
Необходимо реализовать метод таким образом, чтобы он генерировал 
случайное число от 0 до 9, далее считывал с консоли введенное пользователем 
число и выводил, угадал ли пользователь то, что было загадано, или нет. Метод 
запускается до тех пор, пока пользователь не угадает число. После этого 
выведите на экран количество попыток, которое потребовалось пользователю, 
чтобы угадать число.
### Алгоритм решения
В этой задаче ввод значения осуществляется в самом методе. Инициализируем в нем переменную, хранящую количество попыток, и генерацию случайной цифры от 0 до 9, используя класс «Random». Ввод осуществляется по принципу, если это цифра от 0 до 9, то идем дальше, если нет, то необходимо будет ввести ее заново. Получается, если введено число в правильном промежутке, то необходимо увеличить число совершенных попыток. Далее, если введенная цифра совпала со сгенерированной, то нужно вывести количество попыток, если цифра не совпала, то дать еще попытку. Это все выполняется, пока цифры не совпадут. Далее в main вызываем метод.

# Задание 4
## Задача 2
### Текст задачи
Дана сигнатура метода: public int findLast (int[] arr, int x); 
Необходимо реализовать метод таким образом, чтобы он возвращал индекс 
последнего вхождения числа x в массив arr. Если число не входит в массив – 
возвращается -1.
### Алгоритм решения
Сначала вводим количество чисел в массиве, проверяем, что оно корректно и больше 0, в методе «test» с помощью метода «hasNextInt()» и создаем массив с этим количеством. Далее в цикле вводим заданное количество чисел и проверяем, что они корректны, в методе «getInput». Также необходимо ввести число, индекс которого необходимо вывести, тоже осуществляется проверка на это число. В методе, чтобы найти именно последнее вхождение числа в массив, необходимо пройти по массиву с конца (последнего индекса) к началу. Далее нужно совершить проверку, если введенное число совпадает с текущим элементом массива, то возвращаем индекс этого элемента, а если такой элемент не найден, то вернем -1, по условию. Далее в main вызываем метод.

## Задача 4
### Текст задачи
Дана сигнатура метода: public int[]add (int[] arr, int x, int pos); 
Необходимо реализовать метод таким образом, чтобы он возвращал новый 
массив, который будет содержать все элементы массива arr, однако в позицию 
pos будет вставлено значение x.
### Алгоритм решения
Сначала вводим количество чисел в массиве, проверяем, что оно корректно и больше 0, в методе «test» с помощью метода «hasNextInt()» и создаем массив с этим количеством. Далее в цикле вводим заданное количество чисел и проверяем, что они корректны, в методе «getInput». Также необходимо ввести число, которое нужно вставить в массив, и индекс, на какое место необходимо его вставить. По условию должен возвращаться новый массив, поэтому в методе создаем его с длиной изначального массива большей на 1. Необходимо все числа из изначального массива скопировать во второй, поэтому сначала в цикле заносим из первого массива все числа, стоящие до того индекса, куда мы вставляем новое число. После этого вставка введенного числа, а потом снова копируем числа, но уже все, стоящие после индекса. И возвращаем новый массив. Далее в main вызываем метод.

## Задача 6
### Текст задачи
Дана сигнатура метода: public void reverse (int[] arr); 
Необходимо реализовать метод таким образом, чтобы он изменял массив arr. 
После проведенных изменений массив должен быть записан задом-наперед. 
### Алгоритм решения
Сначала вводим количество чисел в массиве, проверяем, что оно корректно и больше 0, в методе «test» с помощью метода «hasNextInt()» и создаем массив с этим количеством. Далее в цикле вводим заданное количество чисел и проверяем, что они корректны, в методе «getInput». Здесь изменяется изначальный массив, не нужно создавать новый. В методе проходим по массиву с конца и выводим элементы. Далее в main вызываем метод.

## Задача 8
### Текст задачи
Дана сигнатура метода: public int[] concat (int[] arr1,int[] arr2); 
Необходимо реализовать метод таким образом, чтобы он возвращал новый 
массив, в котором сначала идут элементы первого массива (arr1), а затем 
второго (arr2).
### Алгоритм решения
Сначала вводим количество чисел в массивах, проверяем, что они корректны и больше 0, в методе «test» с помощью метода «hasNextInt()» и создаем массивы с этим количеством. Далее в цикле вводим заданные количества чисел и проверяем, что они корректны, в методе «getInput». В методе необходимо создать новый массив с длинной, равной сумме длин первых двух массивов. Сначала копируем числа первого массива: проходим по индексам и вставляем в новый массив числа, стоящие на них. Чтобы скопировать числа из второго массива нужно также пройтись по второму массиву, но в новый массив вставлять на места, стоящие после индекса последнего числа 1 массива. Это необходимо, чтобы числа не наслаивались друг на друга. В итоге возвращаем новый массив. Далее в main вызываем метод.

## Задача 10
### Текст задачи
Дана сигнатура метода: public int[] deleteNegative (int[] arr); 
Необходимо реализовать метод таким образом, чтобы он возвращал новый 
массив, в котором записаны все элементы массива arr кроме отрицательных.
### Алгоритм решения
Сначала вводим количество чисел в массиве, проверяем, что оно корректно и больше 0, в методе «test» с помощью метода «hasNextInt()» и создаем массив с этим количеством. Далее в цикле вводим заданное количество чисел и проверяем, что они корректны, в методе «getInput». Нужно создать новый массив, для этого сначала нужно выяснить, какая длина у него должна быть. Для этого пройдем по исходному массиву и посчитаем, сколько в нем положительных чисел. Это количество и будет мощностью массива. Создали новый массив. И снова смотрим, если число в изначальном массиве больше 0, то вставляем это число во второй массив. Возвращаем второй массив. Далее в main вызываем метод.

